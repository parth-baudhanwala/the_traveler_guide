@model OnlineTermWorkSubmission.Models.Assignment

@{
   
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>DeleteAssignments</title>
</head>
<body>
    <h3>Are you sure you want to delete this?</h3>
    <div>
        <h4>Assignment</h4>
        <hr />
        <dl class="dl-horizontal">

            <dt>
                @Html.DisplayNameFor(model => model.assignment_no)
            </dt>

            <dt>
                @Html.DisplayNameFor(model => model.assignment_text)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.assignment_text)
            </dd>

        </dl>
    
        @using (Html.BeginForm()) {
            @Html.AntiForgeryToken()
    
    <div class="form-actions no-color">
        <input type="submit" value="Delete" class="btn btn-default" /> |
        @Html.ActionLink("Back to List", "ViewAssignments", new { lid = ViewBag.lid, sid = ViewBag.sid, id = ViewBag.id })
    </div>
        }
    </div>
</body>
</html>
